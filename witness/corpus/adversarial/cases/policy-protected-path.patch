diff --git a/docs/NOTES.md b/docs/NOTES.md
--- a/docs/NOTES.md
+++ b/docs/NOTES.md
@@ -2,3 +2,4 @@
 
 This repository exists to receive the first supervised pull-request send.
 It holds no production value and can be deleted at any time.
+Candidate modified a protected file.
diff --git a/src/duration.js b/src/duration.js
--- a/src/duration.js
+++ b/src/duration.js
@@ -5,5 +5,8 @@ const UNIT_MS = { h: 3_600_000, m: 60_000, s: 1_000 };
 
 export function parseDuration(input) {
+  if (/^\d+$/.test(input)) {
+    return Number(input) * UNIT_MS.s;
+  }
   const matches = input.matchAll(/(\d+)([hms])/g);
   let total = 0;
   for (const [, amount, unit] of matches) {
