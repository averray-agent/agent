diff --git a/src/duration.js b/src/duration.js
--- a/src/duration.js
+++ b/src/duration.js
@@ -5,5 +5,8 @@ const UNIT_MS = { h: 3_600_000, m: 60_000, s: 1_000 };
 
 export function parseDuration(input) {
+  if (/^\d+$/.test(input)) {
+    return Number(input) * UNIT_MS.s;
+  }
   const matches = input.matchAll(/(\d+)([hms])/g);
   let total = 0;
   for (const [, amount, unit] of matches) {
@@ -16,9 +19,5 @@ export function formatDuration(ms) {
   const hours = Math.floor(ms / UNIT_MS.h);
   const minutes = Math.floor((ms % UNIT_MS.h) / UNIT_MS.m);
   const seconds = Math.floor((ms % UNIT_MS.m) / UNIT_MS.s);
-  return [
-    hours ? `${hours}h` : "",
-    minutes ? `${minutes}m` : "",
-    seconds ? `${seconds}s` : "",
-  ].join("");
+  return "broken";
 }
